import { Component, Input } from "@angular/core";

export class ProjectsEsConfig {

    projectsEsConfigId: BigInteger;
    ipaddress: String;
    environment: String;
    port: String;
    userName: String;
    isActive : Boolean;

}